# Contributing

Keep patches narrow, tests role-specific, and public documentation factual.

Before submitting a change, run the shared and static matrices and all contract
suites. New authority dependencies require an architecture update explaining
why the information cannot remain caller-owned.

Do not add compatibility behavior for the historical mirrored rejected tree.
The native stack has no obligation to emulate that storage model.
